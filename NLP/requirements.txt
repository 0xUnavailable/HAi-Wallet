# NLP Service Dependencies
# Core NLP library for training and inference
spacy>=3.5.0

# Web framework for API endpoints
fastapi>=0.95.0

# Data validation (required by FastAPI)
pydantic>=1.10.0

# ASGI server for running FastAPI
uvicorn[standard]>=0.20.0

# HTTP client for health checks
aiohttp>=3.8.0

# Optional: For better JSON handling and validation
python-multipart>=0.0.6

# Optional: For CORS support in FastAPI
# Note: CORS is built into FastAPI, no additional package needed

# Development dependencies (optional)
# For testing
pytest>=7.0.0
pytest-asyncio>=0.20.0

# For code formatting and linting
black>=22.0.0
flake8>=5.0.0

# For type checking
mypy>=1.0.0

# Note: After installing spacy, you'll need to download the English language model:
# python -m spacy download en_core_web_sm
# 
# For deployment (Render), add this to your build command:
# pip install -r requirements.txt && python -m spacy download en_core_web_sm
# 
# Or if you want to use the custom trained model, make sure the model files
# are present in the model/model-best directory 