// responseFormatter.ts
// Normalizes and formats relay.link API responses for the frontend

export function formatApiResponse(response: any) {
  // Normalize and format the response as needed
  return response;
} 